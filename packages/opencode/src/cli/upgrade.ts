// This distribution has no configured enterprise update source. Keep this startup
// hook as a no-op so upstream orchestration can remain intact without reaching
// OpenCode release services.
export async function upgrade() {}
