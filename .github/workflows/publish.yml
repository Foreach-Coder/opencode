name: publish-disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unavailable:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled publishing
        run: |
          echo "Internal publish destination is not configured."
          exit 1
